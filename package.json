{
  "name": "aptransco-internship-portal",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../admin-portal && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:student": "cd frontend && npm run dev",
    "dev:admin": "cd admin-portal && npm run dev",
    "dev": "npm-run-all --parallel dev:backend dev:student dev:admin"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
